dependencies:
  idf:
    version: ">=5.1"
  lvgl/lvgl: "^8.4.0"
  # ST7796 panel driver — built-in en ESP-IDF v5.x, externo desde v6.0.
  espressif/esp_lcd_st7796: "^1.4.0"
  # Codec audio ES8311 + amp NS4150 del board Waveshare. Sólo para feedback
  # acústico (clicks) en las botoneras de la UI.
  espressif/esp_codec_dev: "^1.5.0"
